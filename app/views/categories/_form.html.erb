<%= form_for(category) do |form| %>

  <div class="form-group">
    <label for="category_name">Nazwa kategorii</label>
    <%= form.text_field :name, placeholder:"Nazwa", :autofocus => true, class:"form-control", required: true %>
  </div>
  <div class="form-group">
    <label for="category_image">Ilustracja</label>
     <%= form.file_field :image, class:"form-control" %>
  </div>
  <div class="actions">
    <%= form.submit "Zatwierdź", class:"btn btn-block btn-success" %>
    <%= link_to 'Lista Kategorii', categories_path, class:"btn btn-block btn-primary" %>
  </div>

<% end %>
