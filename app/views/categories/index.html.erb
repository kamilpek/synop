<% content_for :title, "Lista kategorii" %>

<div class="container-fluid">
  <h3>
    Lista kategorii
    <%= link_to 'Nowa Kategoria', new_category_path, class:"btn btn-sm btn-success" %>
  </h3><hr>

  <div class="table-responsive" align="center">
    <table class="table table-striped">
      <thead class="thead-dark">
        <tr align="center">
          <th>Nazwa</th>
          <th>Akcje</th>
        </tr>
      </thead>

      <tbody>
        <% @categories.each do |category| %>
          <tr align="center">
            <td><%= category.name %></td>
            <td><%= link_to 'Przejdź', category, class:"btn btn-xs btn-success" %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
  <p><%= will_paginate @categories, renderer: WillPaginate::ActionView::BootstrapLinkRenderer %></p>
</div>
