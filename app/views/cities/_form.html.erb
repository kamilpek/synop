<%= form_for(city) do |f| %>
  <% if city.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(city.errors.count, "error") %> prohibited this city from being saved:</h2>

      <ul>
      <% city.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :province %>
    <%= f.number_field :province %>
  </div>

  <div class="field">
    <%= f.label :county %>
    <%= f.number_field :county %>
  </div>

  <div class="field">
    <%= f.label :commune %>
    <%= f.number_field :commune %>
  </div>

  <div class="field">
    <%= f.label :genre %>
    <%= f.number_field :genre %>
  </div>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :name_add %>
    <%= f.text_field :name_add %>
  </div>

  <div class="field">
    <%= f.label :longitude %>
    <%= f.text_field :longitude %>
  </div>

  <div class="field">
    <%= f.label :latitude %>
    <%= f.text_field :latitude %>
  </div>

  <div class="actions">
    <%= f.submit "Zatwierdź", class:"btn btn-block btn-outline-success" %>
  </div>
<% end %>
