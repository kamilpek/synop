<% content_for :title, "Pomiary dzienne" %>

<div class="container-fluid" align="center">

  <h4>
    Lista pomiarów jakości powietrza z dnia <%= @date.strftime("%d.%m.%Y") %>
    <%= link_to "Kalendarz pomiarów", gios_measurments_path, class:"btn btn-sm btn-outline-primary" %>
    <%= link_to gios_measurment_daily_path(date: @date - 1.day), class: "btn btn-sm btn-outline-primary" do %> Poprzedni Dzień <% end %>
    <%= link_to gios_measurment_daily_path(date: @date + 1.day), class: "btn btn-sm btn-outline-primary" do %> Następny Dzień <% end %>
  </h4><hr>

  <div class="table-responsive" align="center">
    <table class="table table-striped">
      <thead class="thead-dark">
        <tr align="center">
          <th>Godzina</th>
          <th>Akcje</th>
        </tr>
      </thead>

      <tbody>
        <% @hours.each do |hour| %>
          <tr align="center">
            <td><%= hour %></td>
            <td><%= link_to 'Przejdź do godziny', gios_measurment_hourly_path(hour: hour, date: @date), class:"btn btn-outline-primary btn-xs" %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>
