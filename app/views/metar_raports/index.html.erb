<% content_for :title, "Depesze METAR" %>

<div class="container">

  <h4>Depesze METAR</h4><hr>

  <div class="row" align="center">
    <div class="col-sm">
      <%= link_to "Poprzedni Miesiąc", metar_raports_path(date: @date - 1.month), class:"btn btn-block btn-outline-primary btn-sm" %>
    </div>
    <div class="col-sm">
      <h5><%= l(@date, format: :calendar).titleize %></h5>
    </div>
    <div class="col-sm">
      <%= link_to "Następny Miesiąc", metar_raports_path(date: @date + 1.month), class:"btn btn-block btn-outline-primary btn-sm" %>
    </div>
  </div>
  <hr>
  <div>
    <%= calendar @date do |date| %>
      <% @days.each do |day| %>
        <%= link_to "#{date.day}", metar_raport_daily_path(1, date: day), class:"btn btn-block btn-outline-primary btn-xs" if date.strftime("%Y-%m-%d") == day %>
      <% end %>
    <% end %>
  </div>

</div>
